## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run build` passes (this also runs `tsc --noEmit`)
- [ ] Followed [STYLEGUIDE.md](../STYLEGUIDE.md) — tokens only, no new colors/fonts,
      chrome stays injected, internal links stay extensionless
- [ ] No new third-party request (the deployed CSP is `connect-src 'none'`)

## Checked in a browser

<!-- Which pages, which browser. Screenshots welcome for anything visual. -->
